@inject NavigationManager NavManager
@code {
    protected override void OnInitialized()
    {
        // Redirige al login y fuerza la recarga para limpiar estados antiguos
        NavManager.NavigateTo("login", forceLoad: true);
    }
}