@page "/"
@rendermode @(new InteractiveServerRenderMode(prerender: false))
@using Microsoft.AspNetCore.Authorization
@using FacturacionVERIFACTU.Web.Models
@using FacturacionVERIFACTU.Web.Services
@attribute [Authorize]
@inject CustomAuthStateProvider AuthStateProvider

<PageTitle>Dashboard - VERIFACTU</PageTitle>

<div class="dashboard">
    <div class="dashboard-header">
        <h1>📊 Dashboard</h1>
        <p>Bienvenido al sistema VERIFACTU</p>
    </div>

    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-icon">👥</div>
            <div class="stat-content">
                <h3>Clientes</h3>
                <p class="stat-number">@clientesCount</p>
                <span class="stat-label">Activos</span>
            </div>
        </div>

        <div class="stat-card">
            <div class="stat-icon">📦</div>
            <div class="stat-content">
                <h3>Productos</h3>
                <p class="stat-number">@productosCount</p>
                <span class="stat-label">En catálogo</span>
            </div>
        </div>

        <div class="stat-card">
            <div class="stat-icon">🧾</div>
            <div class="stat-content">
                <h3>Facturas</h3>
                <p class="stat-number">@facturasCount</p>
                <span class="stat-label">Este mes</span>
            </div>
        </div>

        <div class="stat-card">
            <div class="stat-icon">💰</div>
            <div class="stat-content">
                <h3>Ingresos</h3>
                <p class="stat-number">@totalIngresos.ToString("C")</p>
                <span class="stat-label">Este mes</span>
            </div>
        </div>
    </div>

    <div class="info-section">
        <div class="info-card">
            <h3>✅ Sistema Configurado</h3>
            <p>El sistema VERIFACTU está listo para usar.</p>
            <ul>
                <li>✓ Base de datos PostgreSQL conectada</li>
                <li>✓ Multi-tenancy configurado</li>
                <li>✓ API REST funcionando</li>
                <li>✓ Integración VERIFACTU activa</li>
                <li>✓ Autenticación JWT implementada</li>
            </ul>
        </div>

        <div class="info-card">
            <h3>🚀 Próximos Pasos</h3>
            <p>Comienza a usar el sistema:</p>
            <ol>
                <li>Configura tus datos de empresa</li>
                <li>Añade clientes y productos</li>
                <li>Crea tu primera factura</li>
                <li>Envía facturas a la AEAT</li>
            </ol>
        </div>
    </div>

    @if (currentUser != null)
    {
        <div class="user-info-card">
            <h3>👤 Información de Usuario</h3>
            <table>
                <tr>
                    <td><strong>Email:</strong></td>
                    <td>@currentUser.Email</td>
                </tr>
                <tr>
                    <td><strong>Nombre:</strong></td>
                    <td>@currentUser.NombreCompleto</td>
                </tr>
                <tr>
                    <td><strong>Tenant:</strong></td>
                    <td>@currentUser.TenantId</td>
                </tr>
                <tr>
                    <td><strong>Rol:</strong></td>
                    <td><span class="badge">@currentUser.Role</span></td>
                </tr>
            </table>
        </div>
    }
</div>

<style>
    .dashboard {
        max-width: 1400px;
        margin: 0 auto;
    }

    .dashboard-header {
        margin-bottom: 30px;
    }

    .dashboard-header h1 {
        font-size: 2rem;
        color: #333;
        margin-bottom: 5px;
    }

    .dashboard-header p {
        color: #6c757d;
        font-size: 1.1rem;
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
    }

    .stat-card {
        background: white;
        border-radius: 12px;
        padding: 24px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        display: flex;
        gap: 20px;
        align-items: center;
        transition: transform 0.2s, box-shadow 0.2s;
    }

    .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 4px 16px rgba(0,0,0,0.15);
    }

    .stat-icon {
        font-size: 3rem;
    }

    .stat-content h3 {
        margin: 0 0 8px 0;
        color: #6c757d;
        font-size: 0.9rem;
        font-weight: 600;
        text-transform: uppercase;
    }

    .stat-number {
        font-size: 2rem;
        font-weight: 700;
        color: #333;
        margin: 0;
    }

    .stat-label {
        color: #6c757d;
        font-size: 0.85rem;
    }

    .info-section {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
    }

    .info-card {
        background: white;
        border-radius: 12px;
        padding: 24px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .info-card h3 {
        margin-top: 0;
        color: #667eea;
        font-size: 1.3rem;
    }
</style>

@code {
    [Inject]
    private AuthenticationService AuthService { get; set; } = default!;

    private UserInfo? currentUser;
    private int clientesCount = 15;
    private int productosCount = 48;
    private int facturasCount = 23;
    private decimal totalIngresos = 12450.50m;

    protected override void OnInitialized()
    {
        currentUser = AuthService.GetUser();
    }
}